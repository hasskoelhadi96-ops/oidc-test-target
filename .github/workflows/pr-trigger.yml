name: PR Trigger (fork context)
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  pr-trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "fork PR trigger #7 - custom_branch_policies env bypass test"
